<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="xywx.mapping.CoachDao" >
  <resultMap id="BaseResultMap" type="xywx.dao.Coach" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Apr 26 21:05:15 CST 2016.
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="describes" property="describe" jdbcType="VARCHAR" />
    <result column="detailLink" property="detaillink" jdbcType="VARCHAR" />
    <result column="state" property="state" jdbcType="INTEGER"/>
    <result column="optime" property="optime" jdbcType="TIMESTAMP"/>
    
  </resultMap>
  
  <select id="selectAll" resultMap="BaseResultMap">
  select * from coach where 1=1 
  <if test="name!=null and name != ''">
  and name like '%${name}%'
  </if>
  <if test="state!=null and state != ''">
  and state=#{state}
  </if>
  order by optime desc
  </select>
  
  <select id="selectById" resultMap="BaseResultMap">
  select * from coach where id=#{id}
  </select>
  
  <insert id="insertCoach" useGeneratedKeys="true" keyProperty="id">
  insert into coach(name,describes,detailLink,state,optime) values(#{name},#{describe},#{detaillink},#{state},#{optime})
  </insert>
  
  <update id="updateCoach">
  update coach c set
  <if test="name!=null and name!=''">
  c.name = #{name},
  </if>
  <if test="describe!=null and describe!=''">
  c.describes = #{describe},
  </if>
  <if test="detaillink!=null and detaillink!=''">
  c.detailLink = #{detaillink},
  </if>
  <if test="state!=null and state!=''">
  c.state = #{state},
  </if>
  <if test="optime!=null and optime!=''">
  c.optime = #{optime}
  </if>
  where c.id = #{id}
  </update>
  
  <update id="disableCoach">
  update coach set state = CASE WHEN state = 1 THEN 0 WHEN state = 0 THEN 1 ELSE 1 END WHERE ID = #{id,jdbcType=INTEGER}
  </update>
  
  <delete id="deleteCoach">
  delete from coach where id = #{id}
  </delete>
  
</mapper>